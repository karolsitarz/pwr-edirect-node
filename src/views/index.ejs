<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>PWR-edirect</title>
  <meta property="og:title" content="PWR-edirect">
  <meta property="og:description" content="Proste skróty przekierowań do najważniejszych stron studenta PWR ✨">
  <meta property="og:type" content="website">
  <meta property="og:url" content="https://sitarz.tk/pwr-edirect/">
  <meta name="theme-color" content="#ffffff">
  <style>
    body,html{width:100%;height:100%;margin:0;text-align:center}main{display:flex;flex-direction:column;justify-content:center;align-items:center;height:100%;width:100%;max-width:900px;margin:auto;font-family:sans-serif;font-size:30px}*{position:relative;box-sizing:border-box}
    .links {
      font-size: 0.75em;
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
    }
    .links > div {
      margin: 0.5em;
      display: inline-flex;
      align-items: stretch;
      height: 2.5em;
      flex-shrink: 0;
      border-radius: 0.75em;
      overflow: hidden;
    }
    .links a {
      text-decoration: none;
      color: black;
      display: flex;
      align-items: center;
    }
    .links > div > a:nth-of-type(1) {
      flex-grow: 1;
      padding: 0.25em;
      text-align: left;
      background: #e0e0e0;
      border-radius: 0 0.75em 0.75em 0;
      z-index: 1;
    }
    .links > div > a:nth-of-type(1) > span {
      padding: 0 1em 0 0.5em;
      font-size: 0.85em;
      font-weight: bold;
    }
    .links > div > a:nth-of-type(2) {
      font-size: 0.5em;
      padding: 0 1em 0 2em;
      background: #d1d1d1;
      margin-left: -1.25em;
    }
    .links img {
      flex-grow: 0;
      flex-shrink: 0;
      height: 2em;
      width: 2em;
      align-self: center;
      border-radius: 0.5em;
    }
  </style>
</head>
<body>
  <main>
    <h1>PWR-edirect</h1>
    <div class="links">
      <% Object.keys(data).forEach(endpoint => { %>
        <div>
          <a href="/<%= endpoint %>" class="mainbutton">
            <img src="/<%= endpoint %>/icon-192.png" alt="<%= data[endpoint].name %> - ikona" />
            <span><%= data[endpoint].name %></span>
          </a>
          <a href="/<%= endpoint %>?reset" class="reset">Reset</a>
        </div>
      <% }); %>
    </div>
  </main>
</body>
</html>
